apiVersion: v2
name: cert-manager
description: cert-manager with Gateway API integration (R24) — wraps the upstream cert-manager chart
type: application
version: 0.1.0
dependencies:
  - name: cert-manager
    version: v1.20.3
    repository: https://charts.jetstack.io

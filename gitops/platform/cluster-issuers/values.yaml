# PLACEHOLDER — this is a public repo; no real PII is committed (ADR 0003).
# Lifting the ACME email into a value (vs. hardcoding it in the manifest as the
# pre-migration cert-manager-cluster-issuer.yaml did) is the one behavioural
# gain of making the issuers a chart: set your real address here (or via a
# values overlay kept out of git) before the live standup. Let's Encrypt only
# uses it for expiry/revocation notices.
acmeEmail: admin@example.com

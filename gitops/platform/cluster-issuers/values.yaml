# ACME account email for the Let's Encrypt ClusterIssuers. This is a public
# repo, so this address is public too — Let's Encrypt uses it only for expiry
# and revocation notices. Change it to your own before standing up a cluster
# (a values overlay kept out of git works too, if you'd rather not commit it).
acmeEmail: hello@dackota.com

apiVersion: v2
name: longhorn
description: Longhorn as the default StorageClass (R25, ADR 0002) — wraps the upstream longhorn chart
type: application
version: 0.1.0
dependencies:
  - name: longhorn
    version: 1.12.0
    repository: https://charts.longhorn.io

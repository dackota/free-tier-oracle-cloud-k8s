apiVersion: v2
name: traefik
description: Traefik as the Gateway API implementation (R23, ADR 0004) — wraps the upstream traefik chart
type: application
version: 0.1.0
dependencies:
  - name: traefik
    version: 41.0.1
    repository: https://traefik.github.io/charts
